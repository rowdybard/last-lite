# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lastlite
DB_USER=postgres
DB_PASSWORD=password

# Server Configuration
NODE_ENV=development
PORT=3000

# Render.com PostgreSQL Configuration (for production)
# These will be automatically set by Render when you add a PostgreSQL service
# DATABASE_URL=postgresql://username:password@hostname:port/database
